FROM python:3.12-alpine

WORKDIR /app

RUN pip install --no-cache-dir 'asyncssh>=2.17,<3'

COPY lib /app/lib
COPY services /app/services

ENV PYTHONPATH=/app/lib:/app \
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

CMD ["python", "-m", "services.logger"]
